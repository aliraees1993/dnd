import styled from "styled-components";

const Container = styled.div``;

const WordBox = styled.div`
    margin: 0.25rem 0;
    padding: 0.2rem;
    min-width: 1rem;
`;

const WordWrapper = styled.div`
    display: flex;
    flex-wrap: wrap;
`;

export { Container, WordBox, WordWrapper };
